'use strict'

require('./style.css')

const input = require('./input')
const results = require('./results')

// Returns a component composed of an input field and a results list
module.exports = exports = { input, results }
